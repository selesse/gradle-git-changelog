buildscript {
    repositories {
        maven {
            url uri('../repo')
        }
    }
    dependencies {
        classpath 'com.selesse:gradle-git-changelog:1.0-SNAPSHOT'
    }
}

apply plugin: 'com.selesse.git.changelog'
